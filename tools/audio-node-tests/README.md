# Audio Node host tests

Parser, path, volume, state, emergency, and timeout rules from `protocol/showduino_audio_node.h`. No hardware required.

```text
powershell -File tools/audio-node-tests/run_tests.ps1
```
